p#notice
  = notice
h1
  | キャラクター
table.table.table-hover
  thead.thead-default
    tr
      th
        | Name
      th
        | Picture
      th[colspan="3"]
  tbody
    - @characters.each do |character|
      tr
        td= character.name
        td= character.picture
        td= link_to '表示', character, class: 'btn btn-primary mr-3'
        td= link_to '編集', edit_character_path(character), class: 'btn btn-primary mr-3'
        td= link_to '削除', character, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger mr-3'
= link_to 'キャラクター追加', new_character_path
